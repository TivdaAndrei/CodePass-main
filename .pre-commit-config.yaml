repos:
-   repo: local
    hooks:
    -   id: ai-code-review
        name: AI Code Review (Local)
        entry: python review.py --no-emoji
        language: system
        types: [python]
        pass_filenames: false
